import { BsChevronRight } from "react-icons/bs";
import { IconBaseProps } from "react-icons";

interface Props extends IconBaseProps {}

const RightIcon = (props: Props) => <BsChevronRight {...props} />;

export default RightIcon;
