import { BsChevronLeft } from "react-icons/bs";
import { IconBaseProps } from "react-icons";

interface Props extends IconBaseProps {}

const LeftIcon = (props: Props) => <BsChevronLeft {...props} />;

export default LeftIcon;
